/**
 * Settings feature public API (barrel).
 *
 * Cross-feature imports of settings types must go through this barrel per
 * `eslint-plugin-boundaries` (see `.oxlintrc.json`).
 */

export type { HighlightMode } from "./settings.js";
